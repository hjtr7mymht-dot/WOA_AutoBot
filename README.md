# WOA AutoBot

> 声明：此脚本完全免费。若你通过付费渠道获得，请立即退款并举报。
> 官方交流与反馈：QQ群 1067076460
> 作者网站：https://hjtr7mymht-dot.github.io/   （个人博客与最新动态）
WOA AutoBot 是一款面向 World of Airports 的自动化辅助工具，目标是减少重复点击、提高挂机稳定性，并保留足够的可配置能力，方便不同模拟器和不同机场节奏下使用。
**现已完整支持 macOS（Apple Silicon / Intel）与 Windows 双平台。**

> ⚠️ **v1.5.0 说明**：全面实时识图重构（截图缓存、智能状态机、秒级任务检测）、仪表盘实时倒计时、Aqua Tk 原生剪贴板修复、代码合并精简、深色/浅色无损切换、跨平台全面兼容。

## 项目说明

- 本项目为二创重置项目，原项目链接为 https://github.com/nj-yzf/WOA_AutoBot
- 当前官方仓库为 https://github.com/hjtr7mymht-dot/WOA_AutoBot
- 作者个人网站：https://hjtr7mymht-dot.github.io/
- 路线查找器：https://github.com/hjtr7mymht-dot/ARPA-FOR-WOA   （自动航路规划工具）
- 项目开发过程中，模拟器连接与控制逻辑参考了 https://github.com/LmeSzinc/AzurLaneAutoScript 的部分设计思路

## 软件功能

- 自动扫描并连接安卓模拟器设备（Windows / macOS）
- 支持 ADB 与 uiautomator2 两种触控方式
- 支持 ADB、nemu_ipc（仅 Windows）、uiautomator2、DroidCast_raw 多种截图方案
- 自动处理待机位、地勤、除冰、维修、进近、滑行、起飞等常见流程
- 自动领取地勤、自动购买车辆、自动延时塔台、自动调筛选
- 支持随机任务选择、随机思考时间、随机滑动耗时等防检测配置
- 支持按类别轮换处理飞机（喜爱、机队、其他玩家、活动飞机、客机、货机，多选）
- 支持深色/浅色模式手动切换，主题偏好自动保存
- 支持 2D/3D 游戏视角自动切换（模板匹配检测当前视角状态）
- 支持分辨率自适应坐标缩放（设备物理坐标直连，避免二次缩放偏差）
- 支持方案自检与自动回退到可用的触控/截图组合
- 支持防卡死多次触发后的自动停机保护
- 支持同帧多图批匹配优化，降低高频识别路径 CPU 峰值
- 支持紧凑小窗模式与多开模式
- 支持按实例隔离配置，多个窗口互不覆盖配置文件
- 支持在线公告与使用说明弹窗
- 支持在线公告自动从 GitHub 拉取最新内容（失败则回退本地）
- 支持启动时静默比对在线公告，发现新公告自动弹出
- 支持核心文件完整性防篡改校验（SHA256 指纹，9 个关键文件）
- 支持暂停/继续（临时人工介入后恢复运行，无需重新开始）
- 支持启动时+每小时自动显示醒目赞助公告（GUI 金色加粗渲染）
- 支持自定义点击/滑动随机偏移量（高级设置中可调）
- 支持筛选按钮模板匹配+像素回退双重检测，亮/暗颜色语义自动矫正

## 平台支持

| 平台 | 状态 | 说明 |
|------|------|------|
| **Windows 10/11** | ✅ 完整支持 | 推荐 MuMu12 模拟器，支持 nemu_ipc 高速截图 |
| **macOS (Intel)** | ✅ 完整支持 | 通过 `uv run python gui_launcher.py` 运行，或使用打包的 .app/.dmg |
| **macOS (Apple Silicon)** | ✅ 完整支持 | 原生 arm64 支持，已测试 M 系列芯片 |
| **Linux** | ⚠️ 基础兼容 | ADB 连接可用，部分截图方案需手动配置 |

### macOS 注意事项
- 需要安装 [Android Platform Tools](https://developer.android.com/studio/releases/platform-tools) 或将项目内 `adb_tools/adb` 加入 PATH
- nemu_ipc 截图方案为 MuMu 模拟器专属（仅 Windows），macOS 上自动回退到 ADB 截图
- 推荐使用 uiautomator2 截图方案获得更优性能
- 打包命令：`uv run pyinstaller -y --clean WOA_AutoBot_mac.spec`

## 快速开始

### macOS 用户
1. 下载 `WOA_AutoBot_macOS.dmg`，双击打开
2. 将 `WOA_AutoBot.app` 拖入 Applications 文件夹
3. 首次打开如提示"未识别的开发者"：**右键 → 打开 → 仍要打开**
4. 连接安卓模拟器或真机（需开启 ADB 调试）
5. 在设备下拉框选择目标设备，点击 **启动**

### Windows 用户
1. 解压后运行 `WOA_AutoBot.exe`
2. 连接 MuMu / 蓝蝶 / 雷电等安卓模拟器
3. 在设备下拉框选择目标设备，点击 **启动**

### 源码运行
```bash
# macOS/Linux
uv run python gui_launcher.py

# Windows
python gui_launcher.py
```

## 最近更新

### v1.5.0 (2026-08-06)

**重大更新 — 实时识图重构**
- 🤖 截图缓存引擎：同帧复用 3-5× 加速，避免重复抓取
- 🧠 智能状态机：5 状态（主界面/任务详情/塔台菜单/弹窗/过渡）毫秒级自动识别
- ⚡ 超快速并行任务检测：单帧内一次匹配 7 种待处理图标
- 🚀 主循环空闲间隔 0.12→0.025s（4.8× 提升），有任务间隔 0.02→0.005s（4× 提升）
- 📊 仪表盘升级：新增下次小退/塔台延时倒计时、可用地勤实时数、剩余延时次数
- 📱 定时汇报增强：新增实时状态区块（地勤、塔台、小退倒计时）
- 🖥️ 深色/浅色无损切换：6 类控件实时颜色刷新，无延迟无残影
- 🍎 Aqua Tk 原生剪贴板：移除所有 X11 兼容代码，恢复 macOS Cmd+C/V
- 🧹 代码精简：platform_utils→core/platform、woa_debug→core/debug，消除重复
- 🧹 目录清理：删除 1.6GB 无用文件
- ⚡ 流畅度优化：自适应空闲间隔、日志冷却去重、GC 频率降低
- 🐛 修复地勤短缺恢复 UnboundLocalError、任务冷却缺失
- 🔒 跨平台全面审计通过（Windows/macOS 双平台）

### v1.4.2 (2026-08-01)
- ⏸️ **新增暂停/继续功能** — 主界面与小窗均有按钮，可临时人工介入后恢复运行
- 🎨 **新增每小时赞助公告** — 启动时+每小时自动显示醒目公告（GUI 金色加粗跨平台渲染）
- 🎛️ **新增自定义随机偏移** — 高级设置中可自定义点击/滑动偏移量（1-30px / 1-40px）
- 📡 **新增在线公告更新** — 点击公告按钮自动从 GitHub/jsDelivr/ghproxy 拉取最新公告
- 🐛 **修复进场分配后卡死** — 机位选中后不用旧截图、延长超时、B3 兜底检测 stand_confirm
- 🐛 **修复不起飞轮切** — 塔台全关也进入轮切模式；`=_stable_count` 改为 `+= `修复永远不切换
- 🐛 **修复 ANSI 颜色** — `\033[1;33m` 在 GUI 终端无效，改为 tkinter Tag 自动金色渲染
- 🐛 **修复 CSV 路径** — 写入/读取统一使用 `get_app_data_dir()`，打包后不再指向 .app 内部
- 🐛 **修复 macOS 权限** — `adb_tools/` 和 `platform-tools/` 下 16 个二进制文件自动 `chmod +x`
- 🐛 **修复公告/说明/统计图表** — 文件路径统一、异常不再静默吞没、增加回退路径
- ⚡ **全面加速 40-60%** — 主循环空闲 0.25→0.12s、周期检测 10→5 次、弹窗等待减半等 10+ 处
- ⚡ **优化识图容错** — 筛选 margin 32→36、像素采样 3×3→5×5、阈值 0.08→0.06、颜色容差 80→100
- ⚡ **默认随机偏移翻倍** — 点击 ±3→±6px、滑动 ±4→±8px，各处 click 调用偏移 3-5→6-10

### v1.4.1 (2026-06-17)
- 🐛 **修复不起飞模式轮切Bug**：`stand_only→landing_stand_cycle` 计数器误重置导致进港↔停机坪永不切换
- 🐛 **修复离场领奖漏检**：奖励弹窗在屏幕中央但只在底部ROI搜索，新增奖励区域预扫描
- 🐛 **修复模板中文路径**：`cv2.imread` → `read_image_safe` 支持中文路径
- ✅ **新增公告与说明按钮**：主界面 header_bar 新增 📢公告、📖说明 弹窗
- ✅ **新增ROI尺寸校验**：`_validate_template_roi` 自动检测模板是否超过搜索区域
- ✅ **优化匹配容差**：筛选按钮 margin 24→32、视角按钮 margin 20→28

### v1.4.0 (2026-06-16)
- 🔧 **筛选系统全面升级**：4按钮识图模式 + 模板匹配交叉验证，亮/暗颜色语义自动矫正
- 🖥️ **2D/3D 视角自动切换**：模板匹配检测当前视角，根据配置自动点击切换
- 📂 **类别栏新增客机/货机**：侧边栏类别从4个扩展到6个（喜爱/机队/其他玩家/活动飞机/客机/货机）
- 🔄 **类别栏图标升级**：从单图标改为 on/off 双图标对（亮灰/深灰圆底）
- 🗼 **塔台二次确认窗口**：适配游戏新增的延时确认弹窗（yes.png）
- 🐛 **修复像素回退亮/暗逻辑反转**：模板匹配失败时像素检测不再给出相反的按钮状态
- 🐛 **修复不起飞模式小退问题**：retry 机制 + 等待时间优化
- 🔧 **高级设置滚动条**：支持鼠标滚轮浏览
- 🍎 **macOS 全面兼容**：.app bundle 资源路径、窗口图标 PIL 转换、overrideredirect 守卫

## 在线验证与社区二创说明

当前在线验证策略分为两种模式：

- 官方打包版：采用严格模式。首次使用会执行在线验证；后续仅在发现关键校验模块缺失时再次触发，并拒绝继续执行关键操作。
- 源码运行模式：默认不启用强制阻断，仅保留手动在线验证入口，不会因为在线验证机制阻碍开源社区学习、调试、修改和二创。

这意味着：

- 开源社区直接运行源码，不会被强制在线验证卡住
- 社区二创在保留源码形态时，不会被官方在线验证逻辑强制阻断
- 如果社区自行打包发布，可按自己的发布策略调整验证逻辑与目标仓库

官方打包版额外完整性说明：

- 严格模式下会校验关键模块中的功能守卫标记，以及资助入口与资源目录是否存在。
- 若检测到删除或篡改，会触发在线校验阻断，避免发布包被静默改造后继续分发。

## 联网检测更新说明

- 程序每次启动都会自动进行一次联网版本检测（GitHub Raw -> jsDelivr -> ghproxy）。
- 检测到新版本时会直接弹窗提示，不会自动下载、覆盖或重启。
- 运行过程中不再执行自动更新逻辑，避免覆盖失败或误替换带来的风险。

